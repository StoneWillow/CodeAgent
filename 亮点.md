# CodeAgent 演示亮点

给面试/答辩用：先讲「自己写了控制循环、没用框架」，再讲工具分层与安全。下文按「现在能讲什么 / 以后值得补什么」维护。

---

## 1. 整体设计（开场 30 秒）

- **无 agent 框架**：循环、上下文、工具执行、终止条件都在本仓库；只用 DeepSeek 的 OpenAI 兼容 API。
- **分层迭代**：先 ReAct + 内存上下文 + 流式 CLI，再文件工具与沙箱，再 Bash 权限与人机协同。每层都能讲「为什么先做这个」。
- **模块边界**：`llm/` / `prompts/` / `tools/` / `agent.py` 分开；换模型、加工具、改提示词互不揉在一起。

```text
用户任务 → Conversation 写入
         → Agent 循环：调模型
         → 有 tool_calls 则本地执行，观察写回历史
         → 无 tool_calls 则对用户输出（流式）
         → 硬上限 max_turns，防止空转烧钱
```

---

## 2. 工具设计（当前已实现）

### 注册方式

- 自研 `@registry.tool`（类似 `@tool`）：从函数名、类型注解、docstring 生成 JSON Schema。
- 新工具只注册，**不改 ReAct 循环**。这是可扩展性的核心论点。

### 工具分层：专用工具优先，Bash 垫底

| 层 | 工具 | 设计意图 |
| --- | --- | --- |
| 搜索 | Glob / Grep | 先定位再读，少浪费上下文 |
| 读写 | Read / Write / Edit | Edit 精确替换，避免整文件覆盖 |
| Notebook | NotebookEdit | 按 cell 改 `.ipynb`，不走 Bash 改 JSON |
| 协同 | TodoWrite / AskUserQuestion | 多步有清单；不确定就问人 |
| 执行 | Bash | **只**编译检查、试运行、只读查看 |

**Edit vs Write**：局部改用 Edit（找不到 / 多处匹配会失败并说明次数）；新建或大改才 Write。面试可说「把 diff 语义交给模型，整文件不要进上下文」。

**TodoWrite**：会话内内存列表，按 `id` merge；同时最多一项 `in_progress`。长任务不容易迷失。

**AskUserQuestion**：单选 / 多选 / 自由输入；CLI 阻塞询问，与流式输出错开一行。

---

## 3. 安全与权限（重点讲）

### 3.1 Workspace 沙箱（文件）

- 默认活动范围：项目根下 `workspace/`，**不是**当前目录、也不是源码目录。
- 所有路径经 `Workspace.resolve()`：相对路径拼到 workspace 后 `resolve()`；`..`、外盘、symlink 逃出 → 返回错误字符串，循环不崩。
- Agent 源码、`.env` 在沙箱外，工具碰不到。密钥不入库。

### 3.2 Bash 三级权限：deny > ask > allow

原则：**能用文件工具做的，Bash 一律不准做。** 命令按 `;` `&&` `||` `|` 拆段，**任一段 deny 则整条拒绝**。未匹配白名单视为 **ask**，不默认放行。

| 级别 | 行为 | 典型规则 |
| --- | --- | --- |
| **deny** | 不问，直接拒绝 | 写重定向 `>`/`tee`、rm/cp/mv/mkdir、提权、下载管道执行、`cd ..`、碰 `.env`/`codeagent/`/`.git`、`pip install` |
| **ask** | 终端 `y/N` | `make`/`cmake`/`cargo`、以及一切未匹配命令 |
| **allow** | 直接执行 | `g++`/`python xxx.py`/`pytest`/`git status`/`dir` 等检查与试运行 |

演示建议：

1. 让 Agent 改 `workspace/` 里的代码 → 应走 Edit/Write。
2. 让它 `rm` 或 `echo a > f.txt` → 应立刻拒绝，并提示用专用工具。
3. 让它 `g++` / `python calculator.py` → 应直接跑。
4. 丢一条陌生命令 → 应问你是否允许。

### 3.3 执行约束

- `cwd` 锁定 workspace；超时默认 30s、上限 120s。
- 后台只走 `background=True`，日志在 `workspace/.agent/jobs/`，禁止 `nohup`/`start` 自己挂进程。
- 输出截断，避免一次工具结果撑爆上下文。

---

## 4. 上下文压缩与 Memory（当前）

- **tiktoken 精确计数**：system + memory + 对话 + tools schema 一并计入；默认预算 **1M**（DeepSeek V4）。
- **第一层（≥70%）**：蒸馏全部 `role=tool` 输出——300 行测试日志变成一句「哪条用例、因何失败」；保留 `tool_call_id`，不破坏协议。
- **第二层（仍>35%）**：整段会话压成七段快照；历史变为 `system + [会话快照]`；**本轮不再第三次压缩**。
- **Memory 规则式嵌入**：短期（目标/进度，覆盖写）+ 长期（项目约束，合并写）；每条 `- ` 规则、有字数与条数上限；压缩后完整写入 system。
- **硬上限**：压缩后仍 >100% → CLI 报错退出，不把超限请求发给 API。

## 4.1 错误处理（当前）

- **参数校验**：缺字段/类型不对/半截 JSON → `[error:invalid_args]`，不把 Python 堆栈丢给模型。
- **失败分类**：`denied` / `not_found` / `timeout` / `internal` / `unknown_tool`，便于模型换策略。
- **连续失败熔断**：同一工具同类错误 3 次后 `[error:circuit]`，避免空转烧轮次。
- **工具输出预截断**：单次观察超过约 8k token 先截断，减少顶满上下文。
- **API 有限重试**：超时、429、5xx 指数退避最多 3 次；4xx 不重试。
- **服务端超长**：tiktoken 估计偏差时，API 报 context_length → 强制第二层快照再试一次。
- **压缩失败兜底**：蒸馏 LLM 挂了就截断，不让压缩把主循环打死。

演示：把 `CODEAGENT_CONTEXT_TOKENS=2000`，跑一轮带 Bash/Read 的长任务，终端会出现 `[上下文] 第一层工具蒸馏` / `第二层会话快照`。

---

## 5. 其它交互能力（当前）

- **历史即状态**：消息列表可 JSON 序列化；`sessions/*.json` 落盘，CLI `/list` `/resume` 与 `--web` 多窗口切换。
- **产品层与内核分离**：`SessionStore` + Web 只调 Store/Agent，不改 ReAct 循环语义。
- **流式输出**：`stream=True`，`tool_calls` 客户端拼完整再执行。
- **系统提示**与 **tools schema** 分两条通道。

---

## 6. 以后值得写进本文件的亮点

| 方向 | 为什么值得讲 |
| --- | --- |
| 错误处理与重试 | 已落地：校验、熔断、API 重试、超长再压一次 |
| 会话落盘 / 多窗口 | 已落地：`SessionStore`、`--web` 侧边栏、按内容检索 |
| 更细的 Edit（apply_patch） | 多 hunk、行级补丁，面试可对标 Claude Code |
| Thinking 模式 | DeepSeek `reasoning_content` 必须原样回传，能讲协议细节 |
| 自我校验闭环 | 改完代码强制 `pytest`/`py_compile`，失败继续循环 |
| 子 agent | 只读探索并行，主循环只收摘要 |
| Prompt cache / 费用 | 稳定前缀 + 打印 token，体现工程意识 |
| 人机协同增强 | Ctrl+C 中断、跳过某次工具、插入纠正 |

---

## 7. 答辩时可能被问到的点

- **为什么不用 LangChain？** 题目要求自己写循环与工具；框架会把「你是否理解 agent 怎么转」变模糊。
- **为什么 Bash 那么严？** 文件语义应走结构化工具（可沙箱、可审计）；shell 是万能逃逸口，必须白名单。
- **为什么 deny 不问用户？** 破坏性操作不应依赖用户每次点对；问了也容易误点。
- **未匹配为什么是 ask 不是 deny？** 白名单会漏合理命令（新编译器）；默认拒绝会废掉 agent，默认允许会废掉安全。
- **Edit 失败怎么办？** 观察写回模型（出现次数、未找到），连续 3 次会熔断，让它改策略。
- **为什么工具输出要蒸馏而不是直接删？** 模型需要知道「已经试过什么、结论是什么」；删光会重复犯错。
- **第二层为什么只压一次？** 避免压缩套压缩丢信息；memory 承接长期状态。
